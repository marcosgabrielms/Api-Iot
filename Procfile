# O arquivo 'Procfile' é utilizado pelo serviço de hospedagem (como Heroku) para determinar qual comando deve ser executado para rodar o aplicativo.
# Neste caso, ele indica que o comando para rodar o servidor web Flask é "python app.py".
web: python app.py
